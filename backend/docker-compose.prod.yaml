version: '3.7'
services:
  t-backend:
    image: asia.gcr.io/civic-visitor-357217/t-backend:0.0.4
    platform: linux/x86_64
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
